<%= form_with model: @group, local: true do |form| %>
  <% if @group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@group.errors.count, 'error') %> prohibited this group from being saved:</h2>

      <ul>
        <% @group.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.text_field :name, placeholder: "Category Name", class:"email"%>
  </div>

  <div class="field">
    <%= form.text_field :icon, placeholder: 'Enter the url' %>
  </div>
  
  <div class="action">
    <%= form.submit %>
  </div>
<% end %>
